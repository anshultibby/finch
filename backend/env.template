# LLM Configuration (LiteLLM - supports all providers)
# OpenAI Models: gpt-5, gpt-4o, gpt-4-turbo, gpt-4, etc.
# Anthropic Models: claude-sonnet-4-5-20250929 (Claude 4.5), claude-3-5-sonnet-20241022, claude-3-opus-20240229, etc.
# Google Models: gemini-pro, gemini-1.5-pro, etc.

# Set LLM_MODEL to your preferred model (can be from any provider)
LLM_MODEL=claude-sonnet-4-5-20250929

# OpenAI API Key (Required if using OpenAI models)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (Required if using Claude models)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Google API Key (if using Gemini)
# GOOGLE_API_KEY=your_google_api_key_here

# SnapTrade Configuration (Required)
# Get these from: https://snaptrade.com/dashboard
# Note: "Consumer Key" is also called "Secret" in the dashboard
SNAPTRADE_CLIENT_ID=your_client_id_here
SNAPTRADE_CONSUMER_KEY=your_consumer_key_or_secret_here

# Financial Modeling Prep Configuration (Required)
# Get this from: https://site.financialmodelingprep.com/developer/docs
FMP_API_KEY=your_fmp_api_key_here

# Polygon.io Configuration (Optional - for real-time market data)
# Get this from: https://polygon.io/dashboard/signup
# Free tier: 5 API calls/minute, Starter ($29/mo): 5 calls/second + real-time data
POLYGON_API_KEY=your_polygon_api_key_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000

# Database Configuration (Supabase PostgreSQL)
# Direct connection (works on home network with IPv6)
# Get this from: Supabase Dashboard > Project Settings > Database > Connection String (URI)
# Format: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres
DATABASE_URL=postgresql://postgres:your_password@db.xxx.supabase.co:5432/postgres

# Connection Pooler (works on any network via IPv4)
# Get this from: Supabase Dashboard > Project Settings > Database > Connection Pooling
# Format: postgresql://postgres:[PASSWORD]@[IPv4]:6543/postgres
DATABASE_URL_POOLER=postgresql://postgres:your_password@123.456.789.123:6543/postgres

# Toggle between direct connection and pooler
# Set to "true" to use pooler (recommended for non-home networks or IPv6 issues)
USE_POOLER=false

# Encryption Key (optional - no longer needed for SnapTrade OAuth)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_encryption_key_here

# Supabase Storage Configuration (for image files)
# Get these from: Supabase Dashboard > Project Settings > API
# SUPABASE_URL: Your project URL (e.g., https://xxx.supabase.co)
# SUPABASE_SERVICE_KEY: Service role key (secret, for server-side only)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here
SUPABASE_STORAGE_BUCKET=chat-files

# Chat Configuration (optional)
# Maximum number of historical messages to send to LLM for context (default: 50)
# System prompt is always included in addition to this limit
CHAT_HISTORY_LIMIT=50

# Debug Configuration (optional)
# Set to "true" to save all LLM requests/responses to resources/<session_id>/ for debugging
DEBUG_LLM_CALLS=false

# Set to "true" to save chat conversations to chat_logs/<user_id>/<chat_id>/ for analysis
DEBUG_CHAT_LOGS=false

